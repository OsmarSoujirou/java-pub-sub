spring:
  application:
    name: redis
  devtools:
    restart:
      enabled: true
server:
  port: 8080
redis:
  service:
    host: "sae1.cache.amazonaws.com"
    port: "6379"
cloud:
  aws:
    region:
      static: sa-east-1
    credentials:
      access-key: ""
      secret-key: ""
      session_token: ""

